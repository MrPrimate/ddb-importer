<form class="{{cssClass}}" autocomplete="off" onsubmit="event.preventDefault();">
  <div class="import-file-selector">
    <p class="notes">Select from the list, future updates will allow import from other sources.</p>
    <div>
      <select name="select-package" name="select-package" id="select-package">
        <option></option>
        {{#each packages}}
        <option value="{{this.path}}">{{this.name}}</option>
        {{/each}}
      </select>
    </div>
    <div class="ddb-message import-hidden" id="ddb-message"></div>
    <div class="import-progress import-hidden">
      <div class="import-progress-bar" style="width: 0%"></div>
    </div>
    <div>
      <button class="dialog-button" data-button="import" disabled>
        <i class="fas fa-check"></i>
        {{localize "ddb-importer.adventure-import.button"}}
      </button>
    </div>
  </div>
  <div class="ddb-overlay import-invalid">
    <div class="ddb-working import-hidden"><i class="fas fa-cog"></i><div style="font-size: 15px; text-align: center;">Updating References...</div></div>
  </div>
</form>
