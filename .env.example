# Copy to .env (gitignored) and fill in.
#
# Cobalt token from an authenticated dndbeyond.com session:
# DevTools > Application > Cookies > www.dndbeyond.com > cobalt-token
# Used by `npm run data:config` to refresh data/fallback-config.json.
DDB_COBALT_TOKEN=
