{
  "name": "ddb-importer",
  "version": "0.0.1",
  "scripts": {
    "lint": "npx eslint src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "jest": "^25.1.0",
    "prettier": "2.0.5"
  },
  "dependencies": {
    "typescript": "^3.8.3"
  }
}
